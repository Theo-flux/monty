#include "monty.h"

/**
 * stk_push - opecode function to add to stack
 * @stack: pointer to pointer head of stack
 * @line_number: line number of opecode from file
 * Return: 1 or -1
 */
void stk_push(stack_t **stack, unsigned int line_number __attribute__((unused)))
{
}

/**
 * stk_push - opecode function to add to stack
 * @stack: pointer to pointer head of stack
 * @line_number: line number of opecode from file
 * Return: 1 or -1
 */
void stk_pall(stack_t **stack, unsigned int line_number __attribute__((unused)))
{
}